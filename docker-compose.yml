services:
  postgres:
    image: postgres:16-alpine
    container_name: topcard_db
    environment:
      POSTGRES_USER: topcard
      POSTGRES_PASSWORD: topcard
      POSTGRES_DB: topcard
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
